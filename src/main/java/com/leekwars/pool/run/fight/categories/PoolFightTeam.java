package com.leekwars.pool.run.fight.categories;

import com.leekwars.pool.run.fight.PoolFightBase;

public class PoolFightTeam extends PoolFightBase  {

    public PoolFightTeam(String poolRunId, int seed, int type) {
        super(poolRunId, seed, type);
    }
}
